{
  "name": "@line/construct",
  "version": "0.9.0",
  "description": "The construct CLI: create/refactor/research/import/validate/sync/pipeline over a deterministic, non-LLM architecture engine (@line/construct-core).",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "construct": "./construct.mjs"
  },
  "files": [
    "construct.mjs",
    "version.mjs",
    "dist"
  ],
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@line/construct-core": "^0.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  }
}
