import { Button } from './Button';
import { Container } from './Container';
import { Heading } from './Heading';
import { Image } from './Image';
import { Text } from './Text';

/** Craft's resolver: the block names a saved layout stores (`resolvedName`) and the export reads, mapped to components. */
export const CraftResolver = { Container, Heading, Text, Button, Image };
