export function processAll(items) {
  let total = 0;
  if (items[0]) { total += items[0]; }
  if (items[1]) { total += items[1]; }
  if (items[2]) { total += items[2]; }
  if (items[3]) { total += items[3]; }
  if (items[4]) { total += items[4]; }
  if (items[5]) { total += items[5]; }
  if (items[6]) { total += items[6]; }
  if (items[7]) { total += items[7]; }
  if (items[8]) { total += items[8]; }
  if (items[9]) { total += items[9]; }
  if (items[10]) { total += items[10]; }
  if (items[11]) { total += items[11]; }
  if (items[12]) { total += items[12]; }
  if (items[13]) { total += items[13]; }
  if (items[14]) { total += items[14]; }
  if (items[15]) { total += items[15]; }
  if (items[16]) { total += items[16]; }
  if (items[17]) { total += items[17]; }
  if (items[18]) { total += items[18]; }
  if (items[19]) { total += items[19]; }
  if (items[20]) { total += items[20]; }
  if (items[21]) { total += items[21]; }
  if (items[22]) { total += items[22]; }
  if (items[23]) { total += items[23]; }
  if (items[24]) { total += items[24]; }
  if (items[25]) { total += items[25]; }
  if (items[26]) { total += items[26]; }
  if (items[27]) { total += items[27]; }
  if (items[28]) { total += items[28]; }
  if (items[29]) { total += items[29]; }
  if (items[30]) { total += items[30]; }
  if (items[31]) { total += items[31]; }
  if (items[32]) { total += items[32]; }
  if (items[33]) { total += items[33]; }
  if (items[34]) { total += items[34]; }
  if (items[35]) { total += items[35]; }
  if (items[36]) { total += items[36]; }
  if (items[37]) { total += items[37]; }
  if (items[38]) { total += items[38]; }
  if (items[39]) { total += items[39]; }
  if (items[40]) { total += items[40]; }
  if (items[41]) { total += items[41]; }
  if (items[42]) { total += items[42]; }
  if (items[43]) { total += items[43]; }
  return total;
}
