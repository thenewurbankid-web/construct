version: 1
preset: strict-nextjs

project:
  framework: nextjs
  language: typescript

features:
  root: features

rules:
  CLIENT-001:
    severity: error
    serverOnly:
      - acme-billing

exceptions: []
